# Copie para .env e preencha. O .env é ignorado pelo git — este repositório é
# público, então nenhum segredo pode ser versionado (CLAUDE.md, regra 8).

SUPABASE_PROJECT_ID="<project-ref>"
SUPABASE_URL="https://<project-ref>.supabase.co"

# Chave publishable (anon). Pode ir para o browser — quem protege os dados é o RLS.
# Supabase → Project Settings → API Keys → publishable
SUPABASE_PUBLISHABLE_KEY="sb_publishable_..."

# SEGREDO. Ignora RLS por completo — acesso total de leitura e escrita.
# Supabase → Project Settings → API Keys → service_role
# Só no servidor. Nunca com prefixo VITE_ (o Vite embutiria o valor no bundle).
# Em produção, defina como variável de ambiente da hospedagem, não em arquivo.
SUPABASE_SERVICE_ROLE_KEY="<service-role-key>"

# Espelhos com prefixo VITE_ para o código do client. Só valores públicos aqui.
VITE_SUPABASE_PROJECT_ID="<project-ref>"
VITE_SUPABASE_URL="https://<project-ref>.supabase.co"
VITE_SUPABASE_PUBLISHABLE_KEY="sb_publishable_..."

# ─── Asaas (assinatura RUMVIA Pro, R$ 24,90/mês) ─────────────────────────────
# SEGREDO. Asaas → Integrações → API. Chave de sandbox contém `_hmlg_`.
#
# ⚠️ A chave começa com "$". Use aspas DUPLAS e escape a barra invertida:
#     ASAAS_API_KEY="\$aact_hmlg_..."
# Aspas simples NÃO bastam — o bun expande "$" mesmo dentro delas e a chave
# chega vazia, causando falha silenciosa e confusa.
ASAAS_API_KEY=

# SEGREDO. Você inventa. 32-255 caracteres, sem espaços, NUNCA a API key.
# Vai no header `asaas-access-token` de cada webhook. Gere com:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ASAAS_WEBHOOK_TOKEN=

# Origem pública do app (sem barra no fim). Usada no callback do checkout e para
# montar o endpoint do webhook. Precisa ser HTTPS alcançável pelo Asaas — em dev,
# use um túnel (cloudflared/ngrok).
APP_BASE_URL=https://rumvia.com.br
